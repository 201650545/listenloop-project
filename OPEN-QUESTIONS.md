# OPEN QUESTIONS · 需外部评审决策的核心问题

> 本文档是本次评审的**提问单**。三个产品域都在等决策才能继续开发。
> 回答前请先读完 `README.md` 指向的 5 份设计文档与对应代码；下文的每个问题都建立在仓库内的既有事实之上，**脱离仓库的回答一定跑偏**。

---

## 零、先对齐四条硬约束（决定了什么方案可行）

| 约束 | 事实 | 对设计的含义 |
|:--|:--|:--|
| **C1 手机必须能独立跑** | 手机侧可达国内端点（MiMo / ModelScope / 硅基流动 / 智谱 / 火山方舟），**墙外线路全部不可达**（openrouter / cloudflare / nvidia 实测 000） | 方案不能依赖 PC 网关或墙外免费额度；PC 与手机不在同一网段，局域网模式不通 |
| **C2 语音已经可以做到** | ASR `mimo-v2.5-asr` **0.60s**；TTS `mimo-v2.5-tts` 热态短句 **0.95s** / 200 字符 **3.4s**，且**限时免费** | 语音交互不是"能不能做"，而是"交互怎么设计"；注意 TTS 冷启动 11.5s、无标准 `/v1/audio/transcriptions` 端点（实测 404，必须走 chat 风格接口） |
| **C3 后端没有服务器** | 全部逻辑在 App 本地（`code/lib/`），无云服务、无账号体系 | 涉及"跨设备同步""社交""云端统计"的方案要么排除，要么明确标注为未来可选 |
| **C4 是单人产品，但要有商业价值** | 见 `docs/01_商业与战略规划/01_商业定位与核心价值.md` | 方案要能论证"用户为什么为此付费"，而不是"功能很酷" |

---

## 一、尚雯婕精听法：怎么真正落地

**背景事实**：尚雯婕方法 = 四步（选材 20 分钟新闻访谈 → 听写 → 查证 → **整篇背诵且速度与原声同步**），走完 10 篇可当工作语言用；明确排除影视剧/诗歌类材料。

**仓库现状（这是个必须承认的问题）**：`code/lib/widgets/ai_tutor_sheet.dart` 里那个叫「尚雯婕 3 分钟限时精听法」的功能，实际是**3 道选择题的快测**，与该方法的四步**没有任何对应关系**——只是用了名字。而四步里的**第②步听写**与**第④步同速背诵**，产品里完全不存在。

**要你回答的**：

1. 四步法与产品既有的「逐句精听」是**并存关系还是替换关系**？如果并存，用户在同一个课程里怎么切换而不混乱？
2. 每一步的**量化完成判据**怎么定？我们初步想的是：听写 = 提交率 100%；同速背诵 = ASR 相似度 ≥90% 且时长比 ∈[0.9, 1.1]。这个判据是否成立？有没有更稳的替代（例如用 ASR 词级时间戳做语速对齐，而不是整段相似度）？
3. **听写模式**（最大的一块新功能）的交互细节：提交前锁字幕、提交后做**词级 diff 三色标注**（漏词 / 多写 / 拼写近似），并区分"漏的是功能词（弱读吞音，属必然难点）"与"漏的是实词（需回原声重听）"。这个反馈模型合理吗？中文母语者做英文听写，错误分类还应该补哪几类？
4. 那 3 道选择题：**保留并改名**（它本身是合理的快速检测）还是**直接删**？
5. 商业模式：四步法这种"重度投入"流程，与产品"每天 30 分钟"的定位怎么共存？它会提高还是降低留存？

---

## 二、AI 陪练：命名、功能、语音交互

**现状**：入口叫「AI 智能管家」，面板是三个 tab（3 分钟快测 / 伴学讨论 / Anki 记忆）。"伴学讨论"原先是一套**本地中文模板假回复**（因配置注入 bug 从未真正联网），现在已接通在线大模型。

**要你回答的**：

1. **命名**。「AI 智能管家」被使用者评为"难听"。我方候选：**听伴**（与"精听"同源，两字）/ 陪练（功能直白，偏健身联想）/ 私教（专业，贵气）。请给出你的判断与理由，或提出更好的候选——注意它要和产品名 ListenLoop 的语感一致。
2. **功能重构**。当前是"一个空白聊天框 + 六个快捷提问"，问题是**用户精听时最大的困难是不知道该问什么**，给空白框等于把负担丢回给他。我方思路是改为**以"当前句"为中心**：顶部当前句，中部六个动作（拆音变 / 讲语法 / 造句 / 听写我 / 跟读评分 / 加入闪卡），底部才是自由输入 + 麦克风。请评审这个结构，并回答：六个动作是不是太多？"听写我"与"跟读评分"这两个**让 AI 从"能说"变成"能听"**的动作，交互上怎么做才不是噱头？
3. **语音链路**。基于 C2 的实测数据，给出具体的交互规格：
   - 点击说话 → 录音 → ASR(0.6s) → LLM(7.5s) → TTS(0.95s) 这条链，**端到端约 9 秒**，用户能接受吗？如果不行，怎么改（流式？先出文字后补语音？只朗读关键句？）
   - AI 回复是**默认自动朗读**还是**点击才读**？TTS 冷启动 11.5s 怎么处理（预热时机放在哪一步）？
   - 跟读评分要基于 ASR 的什么输出才可信？只有转写文本（无音素级对齐）时，能评到什么程度、不能评什么？（**请诚实划出能力边界**，不要承诺做不到的）
4. 是否有**免费的语音模型**替代方案值得引入？注意 C1：如果它只在墙外可达，对手机端就等于不存在。

---

## 三、Anki 闪卡：管理入口与学习策略

**现状事实**（来自 `docs/02_工程架构与系统设计/04_AI伴学与Anki记忆卡优化设计.md` 与 `code/lib/models/anki_card_model.dart`）：

- `AnkiCard` 的三个内容字段 `clozeWord` / `phoneticClue` / `aiExplanation` **全是本地中文模板**，AI 从未参与生成；且 `clozeWord` 取的是"句子最后一个词"，**无语义重点**。
- SM-2 算法已实现，但**没有产品层**：无每日上限、无学习步骤、无管理页面。
- 卡片的**唯一入口**藏在 AI 管家面板的第三个 tab 里，没有独立位置可管理。

**要你回答的**：

1. **入口位置**。候选：(a) Library 内二级入口（卡片本就来自课程）；(b) 新增第四个底部 tab（结构不可逆，但心智最清晰）；(c) 课程详情页内的"我的卡片"。请选一个并说明理由，指出另外两个的具体代价。
2. **入卡原则**。我方立场是"必须有证据才入卡"（打星 / 听写错句 / 跟读失败），**不做 AI 批量生卡**，理由是批量生成只会造一座永远不会复习的卡片坟场。这个立场是否成立？如果用户就是想要"一键把整课生卡"，怎么设计才不毁掉复习队列？
3. **策略参数**。给出具体数值并说明依据：每日新卡上限 / 每日复习上限 / 学习步骤（learning steps）/ 毕业与再学习间隔 / 暂停（leech）阈值。注意这是**手机端、单用户、日 30 分钟**的产品，不是 Anki 桌面端的通用场景。
4. **差异化**。我方想做「弱项雷达」：按四大音变（连读 / 失爆 / 弱读 / 闪音）统计错误率并给复习队列加权。这是相对 AnkiDroid 等成熟工具**唯一的竞争理由**——请判断这个判断是否成立；若成立，怎么把它做成用户能一眼看懂的界面？若不成立，真正的差异点应该是什么？
5. **卡面语言**。产品重心已转向**纯英文**。卡面是否**完全不放中文**？若放，放哪一层（提示层？答案层？）？

---

## 四、我们对回答的格式要求

1. **先引用证据再下结论**。凡涉及现状判断，请指出你依据的是哪份文档的哪一节或哪个文件（例如 `code/lib/widgets/ai_tutor_sheet.dart`、`docs/02_.../04_...md` 第几节）。若某处你判断我们需要但仓库里没有，请明说"仓库中未见"。
2. **每个方案给出可执行的交互规格**：界面结构 → 用户操作序列 → 状态与反馈 → 边界情况（失败/离线/无卡/超时）。
3. **必须标注代价与不确定性**：这个方案会牺牲什么、依赖什么假设、哪些地方你没有把握。
4. **商业价值要论证**：不要只说"体验更好"，要说清"这为什么会让人付费或留下来"。
5. **不要重复我方的三个文档已写过的内容**——那些是待评审的方案，不是结论。你的价值在于**指出其中错的地方**。
6. 优先回答**最影响开发顺序**的问题：听写默认逐句还是整段、AI 回复是否自动朗读、闪卡入口选哪个、卡面是否零中文。
