# 任务转交：ListenLoop M1 真机验证（第三次失败后的接手包）

> **写给下一个 Agent**：本文件自包含，不需要回看历史会话。接手后从「§五 下一步行动」开始执行。
> 交接时间：2026-09-25 午后。交接原因：郭老师裁定本任务转交执行。

---

## 一、任务目标（一句话）

在小米 14 真机上完成 **Lesson Creation M1（端侧制课）** 的验收跑：从本地文件导入一个 m4a → 端侧 ASR 转写 → 打包成课 → 开始精听，记录 benchmark 并与 PC 版课程对比；**通过后解封「从链接创建」入口**（该入口此前有意封存，代码在 `lib/creation/` 内）。

**通过标准**（交接文档 HANDOFF-LessonCreation-M1-转接.md §三）：

- benchmark：RTF = 处理时长 ÷ 音频时长、句数、峰值内存、崩溃无；
- 与 PC 版同素材课程对比（文本 / 句数 / 时间轴 / 体验）；
- 结论达到「足够日用」→ 继续 M2/M3。

## 二、当前状态（接手时的事实）

### 代码与质量（无需重做，全部已提交真源仓）

- 最新 commit：`7ecd9f3`（GitHub `201650545/listenloop-project`），全量 `flutter test` **551 项全绿**，`analyze lib test` 0 issue。
- 本会话已落地的功能（都已包含在已装机的 APK 里）：EPIC-04 全部（弱项优先排序 / AI 出题进执行页含第 3 题冲突消解 / .apkg 导出 / 旧 AnkiCard 下线并留导出通道）、lemma 词形归并（三层还原器 + 启动自动迁移 + SP 快照兜底，方案 A 四项按推荐）、顶栏计数器防换行修复、App 图标已换新。
- 设计文档：`docs/02_工程架构与系统设计/` 下 01–13 号全部与代码同步（10 号 §十七~§十九 是最新落地记录）。

### 设备与素材（已核验）

- 设备：小米 14，adb 序列号 `bf6ef967`，USB 在线。
- **最新包已装机**：`com.listenloop.listenloop` versionName=1.0.0，lastUpdateTime=2026-09-25 10:48（含全部新功能与 lemma 迁移）。
- 测试素材：`/sdcard/Download/ted-ed-greek-music.m4a`（4.6MB，与 PC 版同素材，便于对比）+ `/sdcard/Download/gettysburg_test.mp3`（备用）。

### ★ 第三次验证失败（本次要接手的现场）

- **现象**：郭老师按流程操作（Library ＋ → 从本地文件创建 → 选 m4a），**制课失败**。具体失败形态未知。
- **现场证据（2026-09-25 12:10 前后抓取）**：
  - `adb logcat -d` 中**无 FATAL / AndroidRuntime 崩溃、无 flutter error**（缓冲可能已轮转，结论保守看待）；
  - dropbox 无新增 crash 条目（列出的均为历史启动记录，不同 APK 路径对应多次重装）；
  - **推论（待验证）**：失败形态大概率是**应用内错误提示**（CreationScreen 的失败页），而非进程崩溃 —— 失败页会显示 errorCode/errorMessage 与「操作清单」（这是 2026-09-23 修过的能力，失败页信息是完整的，**让郭老师读屏或截图失败页是第一手证据**）。
- **前两次失败历史**（均已修复装机，详见 HANDOFF-LessonCreation-M1-转接.md）：
  1. 首测 `OutOfMemoryError` at `MainActivity.decodeToWav`（ArrayList<Short> 攒全量 PCM 撑爆 256MB Java 堆）→ 已改流式两遍解码（commit `3f33f70`）；
  2. 二测目录缺失失败 → 已修。
- ⚠️ 注意：第三次失败发生在**新包**（含 lemma 迁移 + EPIC-04 全部改动）之后 —— 存量数据迁移（`migrateToLemmaV2`）或新代码引入回归的可能性不能排除，虽然与制课链路理论上无交集（lemma 只动生词本），但**不要想当然排除**。

## 三、环境与操作手册（本机特有，务必照抄）

- **adb 路径**：`D:\android-sdk\platform-tools\adb.exe`（不在 PATH）。
- **flutter**：`D:\flutter\bin\flutter.bat`；Android SDK/JDK 已持久化进 flutter config；**gradle 冷启动可能很慢，构建务必给足 ≥900s 超时**。
- **签名构建 + 装机**（覆盖安装必须带签名注入，否则 INSTALL_FAILED_UPDATE_INCOMPATIBLE）：
  ```
  D:\flutter\bin\flutter.bat build apk --debug --target-platform android-arm64 --android-project-arg=android.injected.signing.store.file="C:\Users\郭永涛\.android\debug.keystore" --android-project-arg=android.injected.signing.store.password=android --android-project-arg=android.injected.signing.key.alias=androiddebugkey --android-project-arg=android.injected.signing.key.password=android
  D:\android-sdk\platform-tools\adb.exe install -r <apk>
  ```
- **MIUI 拦截**：adb install 会弹手机端确认框，**装机时提醒郭老师点「允许」**（否则 INSTALL_FAILED_USER_RESTRICTED）。
- **实时抓日志**（复现时先清后抓）：
  ```
  adb logcat -c
  adb logcat | findstr /i "flutter listenloop sherpa OutOfMemory AndroidRuntime"
  ```
- **Loomy 沙箱 HOME 重定向**：git 提交需 `-c user.name="201650545" -c user.email="yongtaog767@gmail.com"`；推送走 `-c http.proxy=http://127.0.0.1:7890`（GitHub 直连被重置）。
- **真源仓同步**：跑 `D:\Work\gh-sync\_build_repo.py` 重建工作区（docs/ 镜像 Obsidian 库 `D:\Work\AI精听训练器`，code/ 只收 lib/），再 git commit + push。

## 四、失败分析建议顺序（按性价比）

1. **先问郭老师失败页显示什么**（errorCode / errorMessage / 阶段卡在哪）——失败页 2026-09-23 已增强过，信息量大，一句话可能就定位；
2. **引导复现 + 实时抓 logcat**（先 `logcat -c`，复现全程抓）——重点盯 `flutter`、`sherpa`、`decodeToWav`、`OutOfMemory`；
3. **查 App 本地已落盘的课程文件**（若转写完成但打包/入库失败，半成品可能在应用目录）：
   `adb shell "ls -R /sdcard/Android/data/com.listenloop.listenloop/files/ 2>/dev/null | head -40"`；
4. **对照检查 lemma 迁移**：`adb shell "run-as com.listenloop.listenloop cat /data/data/com.listenloop.listenloop/shared_prefs/ListenLoop.xml 2>/dev/null"` 看 `vocab_lemma_migrated` 与生词数据是否正常（迁移异常会拖慢启动但不应影响制课，确认排除即可）；
5. 若是 ASR 阶段失败：检查模型文件是否在位（sherpa-onnx whisper onnx 的 assets/模型路径，见 12 号文档与 `lib/creation/sherpa_onnx_asr_engine.dart`）。

## 五、下一步行动

1. 按上文「失败分析建议顺序」定位第三次失败根因 → 修复 → 重打包装机 → **重新验证**（同流程同素材）；
2. 验证通过 → 记录 benchmark（RTF / 句数 / 峰值内存 / 崩溃无）+ 与 PC 版 `ted-ed-greek-music` 课程对比 → 结论写入开发日志；
3. 通过后**解封「从链接创建」**（入口封存在 `lib/creation/`，恢复 UI 入口 + 走完一轮链接制课真机验证）；
4. 更新 WBS：EPIC-02 完成度（当前 85%）与 M1/M2/M3 里程碑状态；
5. 全程遵守项目纪律：8 定调（听是核心）、附属失败不阻塞核心、破坏性操作先确认、改动先 analyze 后继续。

## 六、本会话已完成工作（避免重复劳动）

| 工作块 | 状态 |
|---|---|
| App 图标换新（方案 A「循环轨道」∞，全套 mipmap/adaptive/monochrome） | ✅ 已装机+像素验证 |
| 顶栏计数器防换行（FittedBox）+ 三图标紧凑化 | ✅ |
| EPIC-04：弱项优先排序 / AI 出题进执行页（含第 3 题冲突消解）/ .apkg 导出 / 旧 AnkiCard 下线留导出 | ✅ |
| lemma 词形归并（三层还原器 / 统一归并键 / 自动迁移+快照） | ✅ 551 测试全绿 |
| 文档：10 号 §十七~§十九 / 13 号设计稿 / 开发日志 9-25 / INDEX | ✅ |
| 真源仓推送：`4f22e19` → `f7cc717` → `1a90e0c` → `7ecd9f3` | ✅ 脱敏复核通过 |

**已知遗留（与 M1 无关，勿混淆）**：
- MIUI 桌面图标缓存需重启手机才显示新图标（App 数据层一切正常）；
- lemma 的 SP 迁移快照（`vocab_items_backup_pre_lemma`）在真机首次启动新包后已生成，保留勿删（回滚兜底）；
- 旧 AnkiCard 调度代码仍留在 `ai_governor_service.dart`（UI 已下线，代码待旧数据清零后再清）。

## 七、关键文档索引

- `docs/02_工程架构与系统设计/11_课程取源通道与YouTube中继运维.md`：取源两条通道与各缺件症状对照表
- `docs/02_工程架构与系统设计/12_手机端自足化_Termux中继.md`：手机 Termux 中继（YouTube 链接制课的依赖）
- `docs/03_研发日志与历史归档/历史技术交接/HANDOFF-LessonCreation-M1-转接.md`：M1 架构与前两次失败
- `docs/开发日志.md`：全量开发史（9-25 条目含本会话全部改动）
- `docs/04_Agent机器索引_快速通道/INDEX.md`：Agent 快速导航 + 交互红线
- `C:\Users\郭永涛\AGENTS.md`：运行时路由与沉淀纪律（P1/P3）
